package com.skilltrack.auth.model;

/**
 * Deprecated: roles are represented by {@link Role}.
 * Kept temporarily to avoid breaking builds if referenced.
 */
@Deprecated
public final class RoleName {
    private RoleName() {
    }
}
